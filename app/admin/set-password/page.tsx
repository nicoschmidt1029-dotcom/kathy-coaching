import { updateAdminPassword } from "@/app/admin/actions";
import { requireAdmin } from "@/lib/admin-auth";

export default async function SetPassword({ searchParams }: { searchParams: Promise<{ error?: string }> }) {
  await requireAdmin();
  const query = await searchParams;
  return <main className="flex min-h-screen items-center justify-center bg-[#f5f0e8] px-4 py-12"><section className="w-full max-w-md rounded-3xl border border-black/10 bg-[#fbf8f2] p-7 shadow-sm sm:p-10"><p className="eyebrow">Katey Coaching</p><h1 className="mt-3 font-display text-4xl text-[var(--plum)]">Neues Passwort</h1><p className="mt-4 text-sm leading-relaxed text-foreground/65">Mindestens 12 Zeichen sowie mindestens einen Buchstaben und eine Zahl verwenden. Dein Browser kann das Passwort anschließend sicher speichern.</p>{query.error && <p className="mt-5 rounded-xl bg-red-900/8 p-3 text-sm text-red-900">Die Passwörter stimmen nicht überein oder erfüllen die Anforderungen nicht.</p>}<form action={updateAdminPassword} className="mt-6 space-y-4"><label className="block text-sm font-medium text-foreground/75">Passwort<input name="password" type="password" minLength={12} required autoComplete="new-password" className="mt-1.5 w-full rounded-xl border border-black/10 bg-white px-4 py-3" /></label><label className="block text-sm font-medium text-foreground/75">Passwort wiederholen<input name="password_confirmation" type="password" minLength={12} required autoComplete="new-password" className="mt-1.5 w-full rounded-xl border border-black/10 bg-white px-4 py-3" /></label><button className="min-h-12 w-full rounded-xl bg-[var(--plum)] px-4 py-3 text-sm font-medium text-white">Passwort speichern</button></form></section></main>;
}
