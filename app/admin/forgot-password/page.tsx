import Link from "next/link";
import { KeyRound } from "lucide-react";
import { requestPasswordSetup } from "@/app/admin/actions";

export default async function ForgotPassword({ searchParams }: { searchParams: Promise<{ sent?: string; error?: string }> }) {
  const query = await searchParams;
  return <main className="flex min-h-screen items-center justify-center bg-[#f5f0e8] px-4 py-8 sm:py-12"><section className="w-full max-w-md rounded-3xl border border-black/10 bg-[#fbf8f2] p-6 shadow-sm sm:p-10"><p className="eyebrow">Katey Coaching</p><h1 className="mt-3 font-display text-4xl text-[var(--plum)]">Passwort vergessen?</h1><p className="mt-4 text-sm leading-relaxed text-foreground/65">Gib deine freigegebene Admin-E-Mail-Adresse ein. Du erhältst einen sicheren Link, über den du direkt ein neues Passwort festlegen kannst.</p>{query.sent && <p className="mt-5 rounded-xl bg-emerald-900/8 p-3 text-sm text-emerald-900">E-Mail gesendet. Öffne bitte nur die neueste Nachricht und klicke dort auf „Choose password“.</p>}{query.error && <p className="mt-5 rounded-xl bg-red-900/8 p-3 text-sm text-red-900">Der Link konnte gerade nicht gesendet werden. Bitte prüfe die E-Mail-Adresse oder versuche es nach einer Minute erneut.</p>}<form action={requestPasswordSetup} className="mt-6 space-y-4"><label className="block text-sm font-medium text-foreground/75">E-Mail<input name="email" type="email" required autoComplete="email" className="mt-1.5 w-full rounded-xl border border-black/10 bg-white px-4 py-3 outline-none focus:border-[var(--clay)] focus:ring-2 focus:ring-[var(--clay)]/15" /></label><button className="flex min-h-12 w-full items-center justify-center gap-2 rounded-xl bg-[var(--plum)] px-4 py-3 text-sm font-medium text-white hover:bg-[var(--plum)]/90"><KeyRound className="size-4" />Link für neues Passwort senden</button></form><Link href="/admin/login" className="mt-6 block text-center text-sm text-foreground/55 hover:text-foreground">Zurück zur Anmeldung</Link></section></main>;
}
