import Link from "next/link";
import { KeyRound } from "lucide-react";
import { requestPasswordSetup } from "@/app/admin/actions";

export default async function ForgotPassword({ searchParams }: { searchParams: Promise<{ sent?: string; error?: string }> }) {
  const query = await searchParams;
  return <main className="flex min-h-screen items-center justify-center bg-[#f5f0e8] px-4 py-8"><section className="w-full max-w-md rounded-3xl border border-black/10 bg-[#fbf8f2] p-6 shadow-sm sm:p-10"><p className="eyebrow">Katey Coaching</p><h1 className="mt-3 font-display text-4xl text-[var(--plum)]">Forgot your password?</h1><p className="mt-4 text-sm leading-relaxed text-foreground/65">Enter your approved Admin email address. We will send you a secure link for choosing a new password.</p>{query.sent && <p className="mt-5 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-900">Email sent. Open only the newest message and select “Choose password”.</p>}{query.error && <p className="mt-5 rounded-xl bg-red-50 p-3 text-sm text-red-900">The link could not be sent. Check the email address or try again after one minute.</p>}<form action={requestPasswordSetup} className="mt-6 space-y-4"><label className="block text-sm font-medium">Email<input name="email" type="email" required autoComplete="email" className="mt-1.5 w-full rounded-xl border border-black/10 bg-white px-4 py-3" /></label><button className="flex min-h-12 w-full items-center justify-center gap-2 rounded-xl bg-[var(--plum)] px-4 py-3 text-sm font-medium text-white"><KeyRound className="size-4" />Send password reset link</button></form><Link href="/admin/login" className="mt-6 block text-center text-sm text-foreground/55">Back to login</Link></section></main>;
}
